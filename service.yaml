apiVersion: v1
kind: Service
metadata:
  name: rivera-service
spec:
  type: NodePort
  selector:
    app: rivera
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30008
